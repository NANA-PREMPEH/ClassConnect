import { renderNav, bindNavEvents } from '../components/nav.js';
import { getAllQuestionBankItems, saveQuestionBankItem } from '../engine/storage.js';
import { showToast } from '../components/ui.js';
const esc = (v = '') => String(v).replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;').replaceAll('"', '&quot;');
export async function renderQuestionEditor() { const items = await getAllQuestionBankItems(); return `${renderNav({ title: 'Question Bank', showBack: true, showLogout: true })}<main class="container container--narrow view-enter cms-page"><div class="card"><h1 class="card__title">Author a question</h1><form id="question-editor-form" class="cms-form"><div class="cms-grid"><select class="input" id="question-type"><option value="mcq">Multiple choice</option><option value="true-false">True / False</option><option value="fill-blank">Fill in the blank</option><option value="short">Short answer</option><option value="code">Coding</option></select><select class="input" id="question-bloom"><option>Knowledge</option><option>Comprehension</option><option>Application</option><option>Analysis</option></select></div><textarea class="input" id="question-prompt" required rows="4" placeholder="Question prompt"></textarea><textarea class="input" id="question-options" rows="4" placeholder="Options, one per line (MCQ only)"></textarea><input class="input" id="question-answer" placeholder="Correct answer / answer key"><label>IRT difficulty (-3 to +3)<input class="input" id="question-difficulty" type="number" min="-3" max="3" step="0.1" value="0"></label><button class="btn btn--primary">Save question</button></form></div><section class="card cms-list"><h2>Saved questions</h2>${items.length ? items.map((item) => `<article><strong>${esc(item.type)} · ${esc(item.bloom)}</strong><span>${esc(item.prompt)}</span></article>`).join('') : '<p class="insight-empty">No custom questions yet.</p>'}</section></main>`; }
export function bindQuestionEditorEvents(navigate) { bindNavEvents({ onBack: () => navigate('/dashboard') }); document.getElementById('question-editor-form')?.addEventListener('submit', async (event) => { event.preventDefault(); const value = (id) => document.getElementById(id).value.trim(); await saveQuestionBankItem({ type: value('question-type'), bloom: value('question-bloom'), prompt: value('question-prompt'), options: value('question-options').split('\n').filter(Boolean), answer: value('question-answer'), difficulty: Number(value('question-difficulty')) }); showToast('Question saved to the local bank.', 'success'); navigate('/question-editor'); }); }
